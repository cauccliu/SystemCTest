#include<iostream>
#include<string>
using namespace std;

// 2024-9-24
// 字符串中最多数目的子序列
/*
字符串text和长度为2的字符串pattern ，两者都只包含小写英文字母。
你可以在text中任意位置插入一个字符，这个插入的字符必须是pattern[0]或者pattern[1],可以插入在text开头或者结尾的位置。
请你返回插入一个字符后，text中最多包含多少个等于pattern的子序列。

子序列指的是将一个字符串删除若干个字符后（也可以不删除），剩余字符保持原本顺序得到的字符串。

只能插入一个字符


 */
long long maximumSubsequenceCount(string text, string pattern) {
    return 0;
}